type Post @model @auth (rules: [
    { allow: owner },
    { allow: public, provider: apiKey, operations: [read] }
]) {
    id: ID!
    slug: String!
    title: String!
    body:String!
    createdAt:String!
}
